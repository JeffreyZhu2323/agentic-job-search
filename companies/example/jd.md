# ACME AI — Applied AI Engineer Intern (FICTIONAL sample)

> Placeholder dream-target folder. `/tune-resume-deep` creates one of these per
> high-priority company: the JD (`jd.md`), the archived tuned resume
> (`resume.pdf` + `resume.tex`), and any company-specific prep. ACME AI is
> fictional; replace with your own targets (those stay private, not committed).

See `eval/jds/sample-ai-engineer.md` for the full sample posting this mirrors.

**Fit notes (example):**
- Strong matches: Python, LLM evals, RAG, PyTorch, agentic tool-use.
- Honest gaps (left off the resume): distributed training at scale, Kubernetes.
- Talking points: the eval-harness project maps directly to their "design offline evals" ask.
